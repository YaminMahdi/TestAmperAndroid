product: android/app

dependencies:
  - $compose.foundation
  - $compose.material
  - $compose.material3
  - $compose.ui
  - $compose.uiTooling
  - $compose.preview
  - $compose.materialIconsExtended
  - $libs.androidx.core.ktx
  - $libs.androidx.lifecycle.runtime.ktx
  - $libs.androidx.activity.compose

repositories:
  - id: jitpack
    url: https://jitpack.io

settings:
  android:
    namespace : "mlab.test.amper"
    compileSdk: 34
    minSdk: 26
  kotlin:
    languageVersion: 2.0
    serialization: json
  compose: enabled